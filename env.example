# GitHub 仓库配置（必需）
# 用于从 GitHub 仓库导入歌单
VITE_GIT_REPO=username/repo
VITE_GIT_BRANCH=main（可选）
VITE_GIT_TOKEN=github-token

# 管理员密码（必需）
# 删除歌曲时必须输入此密码进行验证
VITE_PASSWORD=password

# 应用配置
NODE_ENV=development
